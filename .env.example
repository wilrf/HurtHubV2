# Environment Variables Template
# ===============================
# Copy this file to .env for local development
# Copy this file to .env.production for production values
#
# IMPORTANT: NO NEWLINES IN VALUES - They break parsing!
# IMPORTANT: Never commit .env or .env.production with real keys

# ============================================
# REQUIRED VARIABLES
# ============================================

# OpenAI Configuration
# --------------------
# Get your API key from: https://platform.openai.com/api-keys
# Format: sk-proj-[164 characters total] for new project keys
# Format: sk-[51 characters total] for legacy keys
OPENAI_API_KEY=your_openai_api_key_here

# Supabase Configuration
# ----------------------
# Get these from your Supabase project settings:
# https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Client-side (public) - These are safe to expose
# VITE_ prefix is REQUIRED for client-side variables in Vite
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Server-side only (secret) - NEVER expose this to client
# Used for API routes that need admin access
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ============================================
# VERCEL INTEGRATION VARIABLES
# ============================================
# These are auto-added by Vercel's Supabase integration
# You don't need to set these manually if using the integration

# SUPABASE_SUPABASE_URL=auto_added_by_vercel
# SUPABASE_SUPABASE_ANON_KEY=auto_added_by_vercel  
# SUPABASE_SUPABASE_SERVICE_ROLE_KEY=auto_added_by_vercel

# ============================================
# APP CONFIGURATION
# ============================================

# App Identity
VITE_APP_NAME=Charlotte Economic Development Platform
VITE_APP_VERSION=1.0.0
VITE_APP_ENV=development  # or 'production' for .env.production

# Feature Flags
VITE_ENABLE_AI_FEATURES=true    # Enable AI-powered features
VITE_ENABLE_REAL_TIME=true      # Enable real-time updates
VITE_ENABLE_ANALYTICS=false     # Enable analytics tracking
VITE_ENABLE_EXPORT=true         # Enable data export features

# Development Settings
VITE_DEBUG_MODE=true            # Show debug information
VITE_MOCK_API=false             # Use mock data instead of real API
VITE_SHOW_DEV_TOOLS=true       # Show development tools

# ============================================
# OPTIONAL VARIABLES
# ============================================

# Vercel Deployment (optional for local dev)
# Get from: https://vercel.com/account/tokens
# VERCEL_TOKEN=your_vercel_token_here

# ============================================
# ENVIRONMENT FILE USAGE GUIDE
# ============================================
# 
# 1. LOCAL DEVELOPMENT:
#    - Copy this to .env
#    - Fill in your actual values
#    - Never commit .env to git
#
# 2. PRODUCTION (Vercel):
#    - Add values directly in Vercel Dashboard
#    - OR use .env.production (don't commit with real keys)
#    - Go to: Project Settings > Environment Variables
#
# 3. VARIABLE PREFIXES:
#    - VITE_*: Available in browser (public)
#    - No prefix: Server-side only (secret)
#    - NEXT_PUBLIC_*: Don't use (we're using Vite, not Next.js)
#
# 4. COMMON ISSUES:
#    - Newlines in values: Remove all \n and \r\n
#    - Wrong Supabase project: Check the project ref in URL
#    - Module not found: Run 'npm install' after env changes
#    - Env not loading: Restart dev server after changes
#
# 5. SECURITY:
#    - NEVER commit real API keys
#    - NEVER expose service keys to client
#    - Use .gitignore to exclude .env files
#    - Rotate keys regularly